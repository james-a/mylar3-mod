mylar.db
config.ini
*.py[co]
.idea
logs
.AppleDouble
cache/
custom_exceptions.csv
Thumbs.db
*.torrent
ehtumbs.db
Thumbs.db
lib/comictaggerlib/ct_settings/
settings.json
.DS_Store
version.py
env/
.nova/
*.swp
# git mergetool backup files
*.orig
.ComicTagger
# Testing outputs, caches, etc.
__pycache__
.coverage
# Shared VS Code / Cursor: launch, tasks, extensions are in-repo.
# devcontainer local config volumes (if used)
.devcontainer/config

# Local dev runs (config + db + logs; not for commit)
local-dev-data/
# Snapshot of mylar.db + library for dev rollback (devtools/save_dev_baseline.py)
dev-baseline/
.venv/
.devcontainer/comics
.devcontainer/downloads
