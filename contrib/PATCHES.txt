Fork-only housekeeping wiring (non-exhaustive):

Core:
- data/interfaces/default/manage.html — link "Library Housekeeping" → housekeeping
- data/interfaces/default/housekeeping.html — audit table + Run audit (JSON + JS)
- mylar/webserve.py — housekeeping(), housekeepingAudit() → contrib audit
- contrib/mylar_housekeeping/audit.py — run_library_audit() all series + issues + annuals
- mylar/filers.py — Windows folder_format: use .replace for / → \\ (fixes re.sub + Py3.13+)

Dev (local UI review):
- devtools/bootstrap_local_db.py, seed_dummy_series.py, run_local.ps1, LOCAL_DEV.txt
- .gitignore — local-dev-data/, .venv/

Optional later:
- mylar/api.py — cmd=housekeepingAudit
- Scheduler for periodic audits

Keep each change small; append new rows when you touch more files.
